#!/usr/bin/env bash

# This scripts copies the immutable config files created by Nix into writable temporary config files
# It makes it easier to edit the config files directly and adjust before nixifying them

